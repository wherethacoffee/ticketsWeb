<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Agendar</title>
        <link rel="stylesheet" href="https://bootswatch.com/5/vapor/bootstrap.min.css">
        <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            <a href="/" class="navbar-brand" style="margin-left: 20px;">Grafico del total de status por municipio</a> 
        </nav>

        <form action="/CRUD_admin/graficarMunicipios" method="GET">
            <div class="form-group">
            <label for="municipio" class="form-label mt-4">Municipio</label>
            <select class="form-select" id="municipio" name="nombremunicipio">
                <% if (data) { %>
                    <% for (var i = 0; i < data.length; i++) { %>
                        <option> <%= data[i].nombremunicipio %> </option>
                    <% } %>
                <% } %>
            </select>
            </div>
            <button type="submit" class="btn btn-primary">Generar Gráfica</button>
        </form>
    </body>
</html>